{% extends 'app.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-default">
                <div class="panel-heading">Profile registration</div>
                <div class="form-group">
                    <h3><b>You have successfully registered your profile, enjoy your ride :)</b></h3>
                    <button onclick="location.href = '/';" style="float: left"  id="cancel" class="btn btn-default">OK</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}